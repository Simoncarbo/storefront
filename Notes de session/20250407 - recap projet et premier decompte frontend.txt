hey hey

Petit recap en ecriture directe.

En gros je lance le projet coopia (enfin), a l'aide de toute une série d'outils. 

Ca commence par Django.Le grand, le fort. LE robuste. Le solide.

Avec le tuto j'ai relativement vite compris la logique, et pu obtenir un premier site en phase test local.

Ensuite se sont ajoutés htmx, tailwind et javascript (pas encore alpineJs) pour les aspects frontend.
Par contre pour ca j'utilise surtout chatGPT car c'est beaucoup de boilerplate code, de syntaxe, et ca c'est pas trop mon truc.

Bon et puis comme j'en voulais toujours plus, j'ai voulu utiliser les websockets.
Pour rester en django (car plus solide, facile et durable), j'ai install django-channels.

Franchement, Django channels était assez facile à appréhender. Le concept de consumer est très simple.
Les concepts de channel l ayer et group un peu moins, il faudra que je relise un peu la doc.


Après je suis qd meme encore un peu perturbé par toutes les intéractions et étapes.
Il y a l'url router qui renvoit soit vers une view, soit vers un consumer
La view renvoit vers un template.
Et moyen de transmettre des infos au template via l'url 
(par exemple le nom d'un groupe sur le channel_layer sur base de la room indiquée par l'url)
(l'info pourrait aussi être communiquée plus tard via send message websockets)

Avec Htmx, moyen d'intéragir sans refresh ou sans nouvel url? En fait on passe qd meme par le process url->view->template, mais c'est pas indiqué dans l'url du browser.


Bon, j'ai mtn implem channel layers avec random_send et group_send broadcast automatique et periodique. What's next on the radar?

-> comprendre group versus channel layer DONE (cfr document word)
	self.channel_name = nom/identifiant du consumer
		les messages envoyés à self.channel_name suivent une logique différentes que celle du client, avec la méthode de traitement définie dans 'type' du message dict
	self.channel_layer = logique du channel layer (commun a plusieurs consumers a priori)


CHECK COPILOT FREE VERSION DISPO
Responses are limited to 2,000 code completions and 50 chat messages per month.


-> affichier le décompte pour le group send automatique

Pas si évident cette histoire de décompte.
Le problème c'est la synchronisation entre serveur et client.
Mon pc (serveur) a une horloge mal réglé -> ca sera bien pour tester les mauvaises synchro quand je me connecte par exemple avec mon smartphone.

Sinon pourquoi pas juste envoyer le nombre de secondes qui restent de manière périodique? A priori la communication web est assez rapide
chatgpt tells me not a good idea (people with poor connection will have slightly delay countdown)


todo: 
- check if you need to manage clock desynchronization between server and client (test with smartphone for example)
j'ai fait le test avec Nel et c'est pas synchronisé (diff de ~450 secondes) -> a régler
- make duration of countdown a variable (instead of explicit 10 seconds everywhere)
- Pour le moment le countdown démarre uniquement si un premier common_message a été envoyé. ca pourrait être pas mal de le démarrer plus tôt?





-> pour le random send, avoir le format input pour chaque nouveau message plutôt qu'un text window.