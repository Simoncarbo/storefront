TODO: 
- afficher le nombre de participants?
- synchroniser l'état du process avec clients? (running vs pause vs not running + result + nb participants)
- finish by user
- idea diffusion
- tester sur smartphone


copilot: add a function idea_diffusion() that sends to each member of the group two randomly selected ideas (excluding the idea sent by the member itself)

ok le code a l'air ok
finalement j'envoie 4 idées et au niveau frontend j'en ajoute que 2 mais si il y a un dupliqué il y a une deuxième et troisième chance.

copilot: the blinking of voteStatusText should only apply when statusType == 'tirage'. Remove the blinking if the status changes to another type that is not 'tirage'


Bon pour le moment la diffusion n'est pas affichée au niveau frontend
J'avais oublié de retirer les idées vides pour la diffusion. Mais ca n'explique pas tout.
J'essaie de print les idées en backend et en frontend.

en fait la fonction idea_diffusion n'est pas appelée. Je retire le critère selected_idea = ''.

copilot: self.idea_diffusion(self.ideas) is not called, i don't know why.
ok, il manquait le await, tout simplement.

Les idées étaient affichée avant la fin du tirage au sort -> j'ajoute simulated_processing_time pour attendre des deux côtés.

Ok il y avait un bug j'utilisais const plutôt que let pour la variable ideas_added qui évolue.


Il y a aussi un bug mtn que je ne peux pas finish avec admin et ca fait bugger comme si un process ne se termrinait jamais.
-> c'est parce qu'il faut appeler finish() avant le début du prochain round...
j'ai ajouté le critère !self.is_finished dans la boucle du run_round.

C'est bon ca marche. Max deux idées sélectionnées (pour ca j'ai encore du changer le js code foreach -> for et break plutôt que return

bug: nouveau paragraphe pas coché dans le input menu quand une idée ave nouveau paragraphe est diffusée. L'info n'est meme pas transmise dans le message! A corriger!
