ChatGPT:
This containss only the button. Add a text span on the right of the button. The content of the text can be one of three things: '', 'En attente des autres participants' or 'Fin du vote dans x secondes". Create a javascript function to determine which text is shown

Currently, the text aligns with the top of the voteButton. Make it align with the bottom of the button.

Can you add, at the beginning of the text, when statusType="waiting", a dynamic symbol or animation that symbolizes waiting.


copilot: create a separate class to handle the logic of the voteContainer

chatgpt: When statusType = 'countdown', start a countdown that updates every second. Text should be in red.



reste à faire:
- freeze quand le voteButton a été cliqué (ideaInputs, radio buttons, vote button)
- overlay pendant tirage au sort avec résultat


copilot: create a function where I'll add the on-click logic for voteButton


chatgpt: I want a js function or class that enables to set an overlay over the whole page, and show a text box in the middle of the page with the following centered message: "Tirage au sort en cours...".

chatgpt: when the overlay is activated, leave it for 5 seconds and then remove it. After 3 seconds, show one of these two texts: "Partage d'idées et nouveau vote" or "Un nouveau bout de texte va être ajouté!"


