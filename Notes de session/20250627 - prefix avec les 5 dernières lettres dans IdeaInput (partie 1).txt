enfin ca c'est l'obijectif (le prefix). Mais commençons par revoir ou on en était et voir tout ce qui fonctionne ou pas.

New parargraph et New line fonctionne à merveille. J'aime aussi le styling au niveau result_output. Cool!! :)

copilot: add a function to the ChatLogCommon class that returns the n last characters of the last line, where n is an argument of the function. If the last line contains 0 characters, return ''. If it contains characters, but less than n characters, add '...' to the beginning of the returned string.

OK.

Du coup la classe IdeaInput doit etre connecté à l'instance ChatLogCommon pour pouvoir faire appel à la fonction. Je le mets en argument de son constructeur.
En fait comme c'est IdeasManager qui créé les IdeaInputs, c'est IdeasManager que je dois connecter à ChatLogCommon.

ok le lien ce fait, je vois les prefix printé dans le log. Mais plusieurs problèmes:
- ca met trois petit points meme qd il y a un seul caractere dans la ligne, pas logique
- ca indique aussi le prefix basé sur le "Ici se cosntruit le texte collaboratif, un bout à la fois."
- 5 charactères c'est un peu peu?